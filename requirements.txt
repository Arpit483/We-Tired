# VitalRadar — Python dependencies
# Generated for Raspberry Pi 4 deployment (Python 3.10+)
# Pin minor versions; update patch versions as security fixes are released.
#
# Install:
#   pip install -r requirements.txt
#
# For GPU inference (not needed on Pi 4):
#   pip install torch --index-url https://download.pytorch.org/whl/cu118

# ── Web framework ────────────────────────────────────────
flask>=2.3,<3.0
flask-socketio>=5.3,<6.0
flask-cors>=4.0,<5.0
eventlet>=0.33,<1.0          # async transport for Flask-SocketIO (production)

# ── Sensor communication ─────────────────────────────────
pyserial>=3.5,<4.0

# ── Scientific / ML ──────────────────────────────────────
numpy>=1.24,<2.0
scipy>=1.11,<2.0
torch>=2.0,<3.0              # CPU-only wheel is ~200 MB; install separately on Pi:
                             #   pip install torch --index-url https://download.pytorch.org/whl/cpu

# ── System monitoring ────────────────────────────────────
psutil>=5.9,<6.0

# ── HTTP client (web push from deep_optimized.py) ────────
requests>=2.31,<3.0

# ── Legacy SVM support (not used in production) ──────────
joblib>=1.3,<2.0             # kept in case SVM path is re-enabled
scikit-learn>=1.3,<2.0      # required by joblib pkl files